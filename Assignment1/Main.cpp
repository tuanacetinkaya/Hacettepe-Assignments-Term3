#include <iostream>
#include "Croupier.h"

int main(int argc, char *argv[]) {
    //that is the one, he deals with the GameBoard and take bets ladies and gentleman
    Croupier ourFileGuy("deck.txt", "commands.txt", "output.txt");
//    Croupier ourFileGuy(argv[1], argv[2], argv[3]);
}
